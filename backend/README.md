# Backend

Django backend for tickets and chat messages.

For now, this folder contains only the smallest Django setup and the first
database models.

```txt
core/  Django project settings
app/   Django app for ticket features
```
